spring:
  application:
    name: movierama
  datasource:
    url: jdbc:h2:mem:movieramaDB
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
server:
  port: 8081